# Historical C++ prototype

This directory contains the historical C++ prototype. It speaks the obsolete newline-delimited JSON protocol and is incompatible with the current 4-byte length-prefixed canonical CBOR protocol. It is not built by any active build target and is retained for historical reference only.

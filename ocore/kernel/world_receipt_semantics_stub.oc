module kernel::world_receipt_semantics;

// Historical probe modes link a fail-closed substitute. Only Mode 30 links
// the canonical receipt oracle.
pub unsafe fn start(mode: u64) -> never {
    disable_interrupts();
    loop { halt(); }
}

schema_version = 2
required_gate_count = 24
supplemental_gate_count = 1
portable_command = "./boot-and-test.sh smoke"

[[gate]]
id = "ocore-bootstrap"
required = true
milestone = "M0.1-M0.3"
script = "ocore/kernel/smoke-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "CPL3 entry, SYSCALL return, IRQ0 return, and a later heartbeat execute in QEMU",
  "W^X pages, frame reclamation, typed memory objects, and capability denials pass the bounded bootstrap corpus",
]
nonclaims = [
  "This one-process bootstrap is not multi-process isolation, IPC, or a foreign ABI",
  "It is not evidence of Linux, Plan 9, or a foreign-kernel boot",
]
expected_markers = [
  "O-core kernel: serial online",
  "M03 memory lifecycle: PASS",
  "timer CPL3 return: online",
  "CPL3 heartbeat: online",
  "QEMU smoke: PASS",
]

[[gate]]
id = "world-g2-aarch64-native"
required = true
milestone = "World G2 / AArch64 native compiler"
script = "ocore/kernel/smoke-aarch64-g2-qemu.sh"
evidence_class = "qemu_tcg_aarch64"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "cmp", "git", "shasum", "python3", "qemu-system-aarch64"]
positive_claims = [
  "One O-core kernel compiled for AArch64 retains EL2, enters host EL1, completes one domain-separated HVC return with register and stack integrity, and in one live QEMU TCG run executes native EL0 process, IPC, capability, lifecycle, stale-generation, reclamation, and bounded post-lifecycle counter-progress checks",
]
nonclaims = [
  "This single-vCPU QEMU TCG gate is not physical AArch64, KVM/SVM, SMP, or G3 evidence",
  "It does not boot Linux or Plan 9 and does not establish a general foreign ABI",
  "It provides no PCI or physical-device assignment, DMA isolation, or IOMMU/SMMU evidence",
]
expected_markers = [
  "G2 AArch64 ocorec object: PASS",
  "G2 AArch64 resident EL2 HVC round-trip: PASS",
  "G2 AArch64 EL0 process lifecycle: PASS",
  "G2 AArch64 IPC capability lifecycle: PASS",
  "G2 AArch64 post-lifecycle counter progress: PASS",
  "G2 AArch64 native compiler QEMU smoke: PASS",
]

[[gate]]
id = "world-identity-v1"
required = true
milestone = "World identity PR2 / Mode 27"
script = "ocore/kernel/smoke-world-identity-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "cmp", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "All 20 constitutional World identity atoms have shared typed Rust and O-core definitions with strict nonzero generation, version, term, and index rules",
  "A bounded OWIDENT v1 identity-only corpus converges byte-for-byte between the Rust oracle and native O-core under QEMU; strict decode rejects malformed or zero-valued records, and hierarchical current/reference comparison rejects stale generations and same-generation logical mismatches",
]
nonclaims = [
  "Serialized capability IDs are descriptive non-authority; this gate creates no bearer, CSpace handle, delegation, or authenticated authority",
  "OWIDENT v1 remains the identity-only nested format and does not itself provide OWPROTO framing, transport, schema negotiation, an OValue envelope, a receipt codec, a Governor, or consensus",
  "This repository-conformance slice does not pass G0 or any G0-G13 gate, and QEMU TCG is not physical or hardware-isolation evidence",
]
expected_markers = [
  "World identity Rust/.oc exact-byte convergence: PASS",
  "World identity malformed/nonzero/stale rejection: PASS",
  "World identity v1 native smoke: PASS",
]

[[gate]]
id = "world-protocol-v1"
required = true
milestone = "World protocol PR3 / Mode 28"
script = "ocore/kernel/smoke-world-protocol-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "cmp", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "The architecture-independent OWPROTO v1 record codec uses deterministic big-endian framing, four fixed record kinds, a 16 KiB hard maximum, caller/negotiated record bounds, and strict exact-length, reserved-field, kind, schema, and nested-identity validation",
  "A fixed 20-record, 1254-byte corpus containing two offers, one canonical v1 selection, one disjoint rejection, and all 16 OWIDENT v1 conformance records converges byte-for-byte between the Rust oracle and native O-core under QEMU; version negotiation deterministically selects the highest common version and smaller record limit or an exact contextual rejection",
]
nonclaims = [
  "OWPROTO v1 is a record codec with an offline bounded negotiation function, not a stream or network transport, live peer handshake, authenticated session, encryption, replay protection, membership protocol, or multiplexing layer",
  "Identity and capability descriptions remain inert metadata; decoding or negotiating a record grants no bearer, CSpace handle, delegation, authenticated authority, or ambient process identity",
  "This PR3 slice does not implement PR4 OValue or extension envelopes, PR5 receipts, a Governor, consensus, WorldFS, or Workstream A acceptance, and it passes no G0-G13 gate; QEMU TCG is not physical or hardware-isolation evidence",
]
expected_markers = [
  "World protocol Rust/.oc exact-byte convergence: PASS",
  "World protocol negotiation/canonical rejection: PASS",
  "World protocol bounded malformed rejection: PASS",
  "World protocol v1 native smoke: PASS",
]

[[gate]]
id = "world-value-v1"
required = true
milestone = "World OValue PR4 / Mode 29"
script = "ocore/kernel/smoke-world-value-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "cmp", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "The separate self-framed OWVALUE v1 format freezes an explicit portable allowlist with a 4096-byte record maximum, depth-16 and 128-node limits, deterministic architecture-independent framing, strictly ordered records and scalar-key maps, and a root-only inert versioned extension envelope whose payload must itself be portable",
  "The fixed 19-record, 928-byte corpus (1856 lowercase hex digits; concatenated-corpus SHA-256 264e00550bbbe7561412d9a43f89036667ffbcf27add522131f8e650abef19bc) converges byte-for-byte between the Rust oracle and native O-core under QEMU, with matching SHA-256 over each complete record; canonical encode/decode/reencode is stable, strict decoding rejects malformed or noncanonical values, and hosted projection rejects authority-bearing, capsule, and effectful values",
]
nonclaims = [
  "OWVALUE v1 is inert portable data and admits no capability bearer, CSpace handle, delegation or session token, native capsule, live process, system, or device reference, executable request, or ambient identity; code and object references remain descriptive only",
  "Versioned extension envelopes do not auto-dispatch code, load schemas, rehydrate capsules, resolve authority, or authenticate peers",
  "Mode 29 is an offline codec and hash oracle, not a transport, live M9 crossing, PR5 receipt or signature implementation, execution or grounding convergence result, Governor, consensus, WorldFS, or Workstream A acceptance; it passes no G0-G13 gate, and QEMU TCG is not physical or hardware-isolation evidence",
  "This gate does not make the full hosted src/value.rs OValue enum portable or replace the hosted canonical-CBOR shim wire format",
]
expected_markers = [
  "World value Rust/.oc exact-byte convergence: PASS",
  "World value canonical SHA-256 convergence: PASS",
  "World value core/extension canonical round-trip: PASS",
  "World value authority/capsule rejection: PASS",
  "World value bounded malformed rejection: PASS",
  "World value v1 native smoke: PASS",
]

[[gate]]
id = "world-receipt-v1"
required = true
milestone = "World receipt PR5 / Mode 30"
script = "ocore/kernel/smoke-world-receipt-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "cmp", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "The separate self-framed OWRECEIPT v1 format deterministically binds one bounded canonical execution receipt to exact World identities and generations, SHA-256 content references, descriptive capability rights, terminal and commit fields, evidence-gate identity, and an algorithm-tagged signature envelope",
  "The fixed two-record, 3239-byte conformance corpus (6478 lowercase hex digits; concatenated-corpus SHA-256 1edd90bf881cd42d08e2031482baae4e7c9a95bd78cfa65f0cbe14147c0a2604) converges byte-for-byte between the Rust oracle and native O-core under QEMU, including its 1575-byte current and 1546-byte stale canonical signing preimages; hosted Ed25519 signs and verifies the exact domain-separated preimage and rejects tampering or a wrong key, while native O-core strictly validates the bounded receipt and signature-envelope structure",
]
nonclaims = [
  "OWRECEIPT v1 carries signed descriptive evidence; receipt capability identities and rights are not bearers, CSpace handles, delegation certificates, session tokens, or grants of authority, and signature validity does not establish authorization or current World state",
  "The pinned conformance key is public test material; this slice provides no production key generation, secure storage, hardware binding, enrollment, certificate chain, rotation, revocation, recovery, peer authentication, or trusted-signer policy",
  "Mode 30 proves canonical receipt bytes and signing-preimage convergence under QEMU plus hosted Ed25519 sign/verify; native O-core structurally validates the signature envelope but does not implement or prove a general freestanding Ed25519 verifier",
  "The Mode 30 corpus is constructed offline and does not itself prove live emission; the separate World-project hosted-reference path emits a caller-signed uncommitted OWRECEIPT and Mode 32 consumes it for bounded canonical/semantic comparison, while live-system, KernelWorld, object, capability, and evidence components remain outside that path and the existing O-Git semantic receipt remains a separate unsigned JSON demo",
  "Local generation and commit-field checks are not an authoritative Governor snapshot, replay or commit-fencing service, transport, consensus, WorldFS, or Workstream A acceptance",
  "This gate is not the typed OSTADIX Alpha attestation schema, does not admit evidence into evidence/world_alpha_gates.toml, passes no G0-G13 gate, and QEMU TCG is not physical, hardware-virtualization, or hardware-isolation evidence",
  "OWRECEIPT remains separate from frozen four-kind OWPROTO v1 and OWVALUE v1 and does not change the hosted canonical-CBOR shim wire format",
]
expected_markers = [
  "World receipt hosted Ed25519 sign/verify: PASS",
  "World receipt Rust/.oc exact-byte convergence: PASS",
  "World receipt canonical signing-preimage convergence: PASS",
  "World receipt signature-envelope structural rejection: PASS",
  "World receipt identity/generation/commit-field binding: PASS",
  "World receipt descriptive capability boundary: PASS",
  "World receipt bounded malformed rejection: PASS",
  "World receipt v1 native smoke: PASS",
]

[[gate]]
id = "world-project-runtime-mode32"
required = true
milestone = "World project runtime / Mode 32"
script = "ocore/kernel/smoke-world-project-runtime-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "One focused hosted test enters the coordinator through an exact caller-supplied current-view World launch before workspace or child creation, observes a terminal RuntimeGraph, and emits a caller-signed OWRECEIPT with an unconditional Uncommitted fence",
  "Native Mode 32 fully decodes and exactly re-encodes that live generated receipt, reconstructs its validated signing preimage, independently matches the signer-independent semantic SHA-256, rejects a malformed envelope, clears reused success-only validation tags, and reaches a later timer",
]
nonclaims = [
  "The launch/current view and coordinator observer are caller-supplied descriptive identities, not authenticated Governor membership, admission, authority, reservation, provider placement, dispatch, recovery, or commit",
  "Mode 32 compares canonical receipt structure and semantic content only; it neither executes the project natively nor verifies Ed25519 in freestanding O-core",
  "The hosted execution retains residual HostWorld effects and provides no exactly-once guarantee, remote execution, physical-hardware evidence, G1 passage, Workstream A acceptance, or passage of any G0-G13 gate",
]
expected_markers = [
  "World project runtime hosted fixture: PASS",
  "World project receipt semantic digest preflight: PASS",
  "World project receipt generated-frame ceiling: PASS",
  "World project receipt canonical lowercase hex: PASS",
  "World project receipt full native decode: PASS",
  "World project receipt exact canonical reencode: PASS",
  "World project receipt validated signing preimage: PASS",
  "World project receipt uncommitted fence: PASS",
  "World project receipt semantic structure SHA-256: PASS",
  "World project receipt native canonical/semantic comparison: PASS",
  "World project receipt stale tag reset: PASS",
  "World project receipt boundary: canonical structure only; no native execution or Ed25519 verification",
  "World project receipt native probe: PASS",
  "World project receipt post-test timer: online",
  "World project receipt malformed native rejection: PASS",
  "World project receipt native semantic comparison: PASS",
]

[[gate]]
id = "m02-fault-recovery"
required = true
milestone = "M0.2"
script = "ocore/kernel/smoke-faults-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Eight fresh boots contain the bounded fatal CPL3 fault corpus",
  "A ninth boot recovers a bounded user-copy fault and reaches a later heartbeat",
]
nonclaims = [
  "The one-process fault corpus is not the current kernel ceiling",
  "It does not establish arbitrary fault recovery or multi-process scheduling",
]
expected_markers = [
  "M02 divide: PASS",
  "M02 invalid-opcode: PASS",
  "M02 recoverable user-copy: PASS",
  "M02 fault and recovery matrix: PASS",
]

[[gate]]
id = "m1-process-isolation"
required = true
milestone = "M1"
script = "ocore/kernel/smoke-processes-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Two bounded native processes use separate CR3s and same-VA physical isolation",
  "Exit and fault teardown reject stale identities, reclaim frames, and preserve the sibling",
]
nonclaims = [
  "The gate is single-CPU and does not establish SMP isolation",
  "It is not a general scheduler, IPC, or foreign-process proof",
]
expected_markers = [
  "M1 exit lifecycle: PASS",
  "M1 fault lifecycle: PASS",
  "M1 process isolation and teardown matrix: PASS",
]

[[gate]]
id = "m2-scheduler"
required = true
milestone = "M2"
script = "ocore/kernel/smoke-scheduler-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Four TCBs across two processes exercise bounded single-CPU yield, sleep, wake-once, and timer preemption",
  "One million forced identity transactions and lifecycle reclamation pass",
]
nonclaims = [
  "The gate does not establish SMP safety or an unbounded production scheduler",
  "The million-transaction phase does not itself enter CPL3",
]
expected_markers = [
  "M2 forced identity transactions: 1000000 PASS",
  "M2 timer preemption: PASS",
  "M2 scheduler lifecycle: PASS",
  "M2 post-lifecycle timer: online",
  "M2 scheduler smoke: PASS",
]

[[gate]]
id = "m3-ipc-foundation"
required = true
milestone = "M3 foundation"
script = "ocore/kernel/smoke-ipc-foundation-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Kernel-side shared mapping, bounded FIFO/cancel, waiter cleanup, endpoint generation, and attenuating-transfer mechanisms pass",
  "The bounded foundation reclaims its resources and reaches a later timer",
]
nonclaims = [
  "This foundation gate is not the public CPL3 IPC gate",
  "It does not establish a general foreign personality or distributed IPC",
]
expected_markers = [
  "M3 foundation shared mapping: PASS",
  "M3 foundation attenuating transfer: PASS",
  "M3 foundation resources reclaimed: PASS",
  "M3 foundation post-lifecycle timer: online",
  "M3 IPC foundation smoke: PASS",
]

[[gate]]
id = "m3-public-ipc"
required = true
milestone = "M3"
script = "ocore/kernel/smoke-ipc-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Four CPL3 processes exercise public endpoint calls, bounded blocking, request/reply, and exact attenuated capability transfer",
  "Personality failure is contained while an unrelated world survives and all bounded resources are reclaimed",
]
nonclaims = [
  "The fixed-capacity single-CPU gate is not unbounded or SMP IPC",
  "It does not implement a Linux or other foreign ABI",
]
expected_markers = [
  "M3 public CPL3 endpoint syscalls: PASS",
  "M3 personality crash containment: PASS",
  "M3 unrelated world after crash: PASS",
  "M3 resources reclaimed: PASS",
  "M3 native IPC smoke: PASS",
]

[[gate]]
id = "m4-native-loader"
required = true
milestone = "M4"
script = "ocore/kernel/smoke-loader-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "llvm-objdump", "nm", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "A deterministic OVFS image imports two separately linked native ELF personalities as data",
  "Malformed and W+X corpus entries are rejected before isolated loads, service lookup, teardown, and frame reclamation",
]
nonclaims = [
  "The bounded read-only OVFS is not a general filesystem or dynamic linker",
  "The native test personalities are not Linux, Plan 9, or another foreign OS",
]
expected_markers = [
  "M4 OVFS image import: PASS",
  "M4 ELF rejection corpus: PASS",
  "M4 loaded address-space W^X: PASS",
  "M4 resources reclaimed: PASS",
  "M4 native loader smoke: PASS",
]

[[gate]]
id = "m5-native-live"
required = true
milestone = "M5"
script = "ocore/kernel/smoke-live-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "nm", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Four packaged native ELFs are hash-verified, loaded into isolated CSpaces, and health-gated before publication",
  "A package-daemon fault withdraws generation 1, preserves unrelated services, and republishes a healthy generation 2 before reclamation",
]
nonclaims = [
  "The bounded control plane is not a general package manager or unbounded retry policy",
  "It does not boot or supervise a foreign operating system",
]
expected_markers = [
  "M5 serial package activation: PASS",
  "M5 pkgd fault containment: PASS",
  "M5 pkgd restart generation: PASS",
  "M5 resources reclaimed: PASS",
  "M5 native live smoke: PASS",
]

[[gate]]
id = "m5-supervisor-semantics"
required = true
milestone = "M5 semantics"
script = "ocore/kernel/smoke-live-semantics-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "The bounded native state corpus covers immutable roots, overgrant denial, failed-health nonpublication, rollback, stale references, and strict parsing",
  "Crash and restart preserve explicitly unrelated state and reach a post-test tick",
]
nonclaims = [
  "The self-test corpus is not the separate interactive M5 service-process gate",
  "It does not establish a general backoff, durability, or foreign-service policy",
]
expected_markers = [
  "M5 overgrant/incomplete activation denial: PASS",
  "M5 complete-set rollback + stale refs: PASS",
  "M5 supervisor semantics: PASS",
  "M5 semantics post-test tick: online",
  "M5 native semantics smoke: PASS",
]

[[gate]]
id = "m6a-scalar-personality"
required = true
milestone = "M6A"
script = "ocore/kernel/smoke-personality-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "objdump", "nm", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Four packaged CPL3 ELFs exercise health-gated scalar personality RPC, deterministic terminal arbitration, and one supervised restart",
  "Generation-1 authority stays stale after generation-2 rebind while an unrelated observer survives and resources return to baseline",
]
nonclaims = [
  "Pointer-bearing calls and request-scoped foreign memory views are disabled",
  "The native test personality is not a Linux or other foreign operating-system ABI",
]
expected_markers = [
  "M6A g1 scalar RPC corpus: PASS",
  "M6A g2 replacement/rebind: PASS",
  "M6A stale/late/duplicate rejection: PASS",
  "M6A resources reclaimed: PASS",
  "M6A scalar personality smoke: PASS",
]

[[gate]]
id = "m6b-bounded-copy"
required = true
milestone = "M6B mechanism"
script = "ocore/kernel/smoke-m6b-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Generation-tagged bounded-copy request views enforce snapshot input, written-prefix output, typed rights, quotas, and revoke-before-terminal ordering",
  "Five delegated lease classes support transactional create-bind rollback and request-wide revocation while unrelated scope survives",
]
nonclaims = [
  "The mechanism is not integrated with the live M6A CPL3 RPC path",
  "It does not establish pinned windows, streaming, signals, a Linux oracle, or concrete delegated services",
]
expected_markers = [
  "M6B bounded snapshot + attenuated view caps: PASS",
  "M6B revoke-before-terminal + wake-once paths: PASS",
  "M6B delegated lease bind-failure rollback: PASS",
  "M6B request-wide revoke + unrelated-scope survival: PASS",
  "M6B bounded native mechanism slice: PASS",
  "M6B bounded native mechanism smoke: PASS",
]

[[gate]]
id = "m6b-live-bounded-personality"
required = true
milestone = "M6B Mode 24 live"
script = "ocore/kernel/smoke-live-bounded-personality-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "objdump", "nm", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "Four digest-pinned CPL3 ELFs exercise one-shot four-byte INOUT bounded personality RPC across health-gated publication, one contained daemon fault, and a generation-2 rebind",
  "The live terminal corpus covers cancellation, timeout, service death, and supervisor-triggered pre-terminal unmap, request-revoke, delegated-resource-revoke, and caller-exit dispositions with stale and duplicate denial plus bounded cleanup",
]
nonclaims = [
  "Mode 24 is a native test personality, not a Linux or Plan 9 boot, general foreign ABI, or general guest-agent path",
  "The generation-2 lifecycle operations are supervisor-triggered pre-terminal dispositions; the gate does not mutate a mapping, observe an external resource event, or cover the post-reply/pre-consume process-exit or unmap race",
  "The delegated device resource is one internal typed lease; this is not KVM, PCI or physical-device assignment, DMA, IOMMU, interrupt-remapping, or physical-device evidence",
]
expected_markers = [
  "M6B live OVFS image import: PASS",
  "M6B live direct saved-RAX result/no reissue: PASS",
  "M6B live bounded fault containment: PASS",
  "M6B live personality g2 cooperative stop: ready",
  "M6B live supervisor policy loop: PASS",
  "M6B live bounded terminal matrix + g2 rebind: PASS",
  "M6B live CPL3 requests/admin lifecycle dispositions: PASS",
  "M6B live bounded authority cleanup: PASS",
  "M6B live post-lifecycle timer: online",
  "M6B live bounded personality smoke: PASS",
]

[[gate]]
id = "m6-linux-minimal-live"
required = true
milestone = "M6 Linux Mode 25 live"
script = "ocore/kernel/smoke-live-linux-personality-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "file", "objdump", "nm", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "One exact digest-pinned static Linux x86-64 ELF and three native service principals load from immutable OVFS data into isolated CPL3 address spaces",
  "Bounded fd 1/fd 2 writes, exact -ENOSYS, and exit_group(42) survive one contained daemon fault, health-gated generation-2 replacement, stale generation-1 denial, and complete authority/resource reclamation",
]
nonclaims = [
  "The pinned four-call success path, with a fifth failure-only exit site, is not Linux or Plan 9 boot, a distribution, root filesystem, dynamic linker, general foreign ABI, or arbitrary Linux binary compatibility",
  "QEMU TCG CPL3 execution is not KVM/SVM or physical-hardware evidence",
  "The gate has no PCI or physical-device assignment, DMA mapping/isolation, IOMMU isolation, interrupt remapping, or hardware reset",
]
expected_markers = [
  "M6 Linux OVFS image import: PASS",
  "M6 Linux four packaged ELF loads: PASS",
  "o-core linux stdout",
  "M6 Linux bounded fault containment: PASS",
  "M6 Linux unrelated world survived: PASS",
  "M6 Linux personality daemon ELF g2: online",
  "M6 Linux prior-generation request rejection: PASS",
  "o-core linux stderr",
  "M6 Linux exact -ENOSYS return: PASS",
  "M6 Linux direct exit_group(42): PASS",
  "M6 Linux supervisor policy loop: PASS",
  "M6 Linux bridge/fd generation cleanup: PASS",
  "M6 Linux resources reclaimed: PASS",
  "M6 Linux post-lifecycle timer: online",
  "M6 live Linux personality smoke: PASS",
]

[[gate]]
id = "m7-linux-plan9-9p2000-live"
required = true
milestone = "M7 Linux/Plan 9 Mode 26 live"
script = "ocore/kernel/smoke-live-linux-plan9-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "file", "objdump", "nm", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "One exact digest-pinned static Linux x86-64 ELF, an unprivileged native Linux 9P2000 server, a native supervisor, and a Plan-9-style native 9P2000 client load from immutable OVFS data into four isolated CPL3 address spaces",
  "The Linux ELF's bounded stdout/stderr results are read through exact 9P2000 version, attach, walk, open, read, and clunk exchanges at /srv/linux/status across namespace withdrawal, one contained server fault, generation-2 replacement, stale generation-1 denial, and complete resource reclamation",
]
nonclaims = [
  "Mode 26 executes the same bounded Linux-ABI ELF and a native O-core Plan-9-style client; it does not boot Linux or Plan 9, run a Plan 9 binary, provide a distribution, root filesystem, or dynamic linker, or establish a general foreign ABI",
  "The exact 128-byte 9P2000 corpus exposes only the generation-bound /srv/linux/status path; it is not a general 9P server, Plan 9 namespace or mount environment, network transport, persistent filesystem, or guest-agent framework",
  "Generation 2 is the same server implementation serving a later, different snapshot after generation 1 completed; this is not two-provider routing for one immutable object, requester-local fallback, fresh provider-B session/fid reconstruction, causal multi-attempt tracing, or live OWRECEIPT emission",
  "QEMU TCG CPL3 execution is not KVM/SVM or physical-hardware evidence",
  "The gate has no PCI or physical-device assignment, DMA mapping or isolation, IOMMU isolation, interrupt remapping, or hardware reset",
]
expected_markers = [
  "M7 Linux/9P OVFS image import: PASS",
  "M7 Linux provider + Plan 9 CPL3 loads: PASS",
  "M7 Linux/9P address-space W^X: PASS",
  "M7 Linux/9P isolated principal CSpaces: PASS",
  "M7 9P private-before-health publication: PASS",
  "M7 Plan 9 client ELF: online",
  "M7 Linux-to-9P namespace path: armed",
  "o-core linux stdout",
  "M7 Linux 9P snapshot g1: PASS",
  "M7 Linux 9P service g1: PASS",
  "M7 9P2000 Linux namespace g1: PASS",
  "M7 provider fault withdrew 9P namespace: PASS",
  "M7 Linux and Plan 9 clients survived fault: PASS",
  "M7 Linux 9P daemon g2: online",
  "M7 Linux 9P stale g1: PASS",
  "M7 stale 9P call capability denial: PASS",
  "o-core linux stderr",
  "M7 Linux 9P snapshot g2: PASS",
  "M7 Linux 9P service g2: PASS",
  "M7 9P2000 Linux namespace g2: PASS",
  "M7 Linux provider exact -ENOSYS return: PASS",
  "M7 Linux provider direct exit_group(42): PASS",
  "M7 Linux supervisor policy loop: PASS",
  "M7 Linux/9P generation cleanup: PASS",
  "M7 Linux/9P resources reclaimed: PASS",
  "M7 Linux/9P post-lifecycle timer: online",
  "live Linux/Plan 9 9P2000 lifecycle: PASS",
]

[[gate]]
id = "m7b-logical-read-fallback-live"
required = true
milestone = "M7B-1 native LogicalRead Mode 31"
script = "ocore/kernel/smoke-m7b-logical-read-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "file", "objdump", "nm", "shasum", "python3", "qemu-system-x86_64"]
positive_claims = [
  "One deterministic provider ELF is instantiated as two generation-distinct isolated CPL3 provider principals; distinct A/B service bindings, endpoints, and client call capabilities are admitted before one requester-local LogicalRead for an exact immutable 20-byte object",
  "Provider A returns a valid terminal 9P Rerror, faults, and has its local route and call authority withdrawn; the client proves A stale before staged provider-B activation, then completes a fresh B-local version/attach/walk/open/read/clunk sequence with different fids, verifies the pinned SHA-256, and reaches separate cleanup, full reclamation, witness-survival, and post-timer evidence",
]
nonclaims = [
  "This is the bounded M7B-1 local mechanism, not complete M7B: requester and router are one principal, both providers instantiate one implementation artifact, and the route set is fixed local configuration rather than a general route registry",
  "The kernel causal state and serial transcript are non-persisted unsigned diagnostics, not a live OWRECEIPT, attestation, Governor commitment, lease protocol, or distributed consensus evidence",
  "The exact read-only 9P2000 corpus is not general 9P, WorldFS, a writable filesystem, fid migration, exactly-once effects, network transport, persistence, Linux or Plan 9 boot, or a foreign KernelWorld",
  "Forced QEMU TCG CPL3 execution is not KVM/SVM, physical-hardware, G7/G8, PCI/device assignment, DMA/IOMMU isolation, interrupt-remapping, or hardware-reset evidence",
]
expected_markers = [
  "M7B-1 OVFS immutable corpus: PASS",
  "M7B-1 one provider ELF, two CPL3 instances: PASS",
  "M7B-1 four isolated native CSpaces/address spaces: PASS",
  "M7B-1 A/B identities and distinct routes admitted: PASS",
  "M7B-1 local 9P fragment path: armed",
  "M7B-1 provider A: online",
  "M7B-1 client LogicalRead: online",
  "M7B-1 witness unrelated native principal: online",
  "M7B-1 provider A read: unavailable",
  "M7B-1 provider A Rerror consumed: PASS",
  "M7B-1 provider A: deliberate fault",
  "M7B-1 provider A route withdrawal/stale generation: PASS",
  "M7B-1 stale provider A capability: PASS",
  "M7B-1 fallback A to B after withdrawal: PASS",
  "M7B-1 provider B staged activation after stale proof: PASS",
  "M7B-1 provider B: online",
  "M7B-1 provider B immutable read: PASS",
  "M7B-1 provider B immutable 20-byte read: PASS",
  "M7B-1 native LogicalRead SHA-256: PASS",
  "M7B-1 native LogicalRead fallback: PASS",
  "M7B-1 non-persisted causal attempt (not OWRECEIPT): PASS",
  "M7B-1 provider A physical/process cleanup: PASS",
  "M7B-1 provider B session/queues cleanup: PASS",
  "M7B-1 bounded mechanism resources reclaimed: PASS",
  "M7B-1 post-lifecycle timer: online",
  "Mode 31 M7B-1 LogicalRead smoke: PASS",
]

[[gate]]
id = "kernel-world-mode20-objects"
required = true
milestone = "KernelWorld Mode 20"
script = "ocore/kernel/smoke-kernel-world-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "The exact hash-pinned V2 record is parsed under default-deny package, manifest, request, export, and typed-rights binding",
  "Generation-bound nonexecuting VM, vCPU, and guest-page objects enforce quota, stale denial, exact-world reclaim, and unrelated-VM survival",
]
nonclaims = [
  "Mode 20 does not enter a guest, execute firmware, or publish a provider export",
  "It does not establish device assignment, DMA mapping, or IOMMU isolation",
]
expected_markers = [
  "KW pinned hash/tamper + noncanonical-order rejection: PASS",
  "KW bounded VM pilot configured without execution: PASS",
  "KW exact-world revoke/reclaim; unrelated VM survives: PASS",
  "KW native admission + nonexecuting VM objects: PASS",
  "KernelWorld native admission/object-model smoke: PASS",
]

[[gate]]
id = "kernel-world-mode21-svm-kvm"
required = false
milestone = "KernelWorld Mode 21"
script = "ocore/kernel/smoke-kernel-world-execution-qemu.sh"
evidence_class = "hardware_kvm"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "grep", "python3", "qemu-system-x86_64"]
positive_claims = [
  "On an AMD host with nested SVM/NPT and writable /dev/kvm, KVM enters a bounded synthetic guest and observes controlled hypercall and interrupt exits",
  "An unmapped guest-physical access fails closed before exact NPT teardown, vCPU restart, and unrelated-VM survival",
]
nonclaims = [
  "Mode 21 is supplemental hardware-dependent evidence and is not part of the portable release aggregate",
  "It does not boot Linux, Plan 9, firmware, or a supplied image",
  "It has no provider lifecycle, guest agent, service export, virtual device, PCI assignment, DMA mapping, or IOMMU-isolation proof",
]
expected_markers = [
  "KW virtualization capability detected: PASS",
  "KW vCPU entered guest mode: PASS",
  "KW controlled hypercall exit: PASS",
  "KW unauthorized guest memory denied: PASS",
  "KW exact-world NPT teardown: PASS",
  "KW first executable VM substrate: PASS",
  "KernelWorld first generation-bound vCPU execution smoke: PASS",
]

[[gate]]
id = "kernel-world-mode22-live"
required = true
milestone = "KernelWorld Mode 22"
script = "ocore/kernel/smoke-kernel-world-live-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "The TCG-compatible native administrative gate health-publishes exact typed exports and dispatches bounded reset intent",
  "Failure withdraws clients before exact VM-graph revoke; policy restarts generation 2 while unrelated service survives and generation 1 stays stale",
]
nonclaims = [
  "Mode 22 does not enter a guest or enforce the manifest health timeout",
  "It has no Linux or Plan 9 boot, guest agent, shared queue, device assignment, DMA/IOMMU isolation, or hardware reset",
]
expected_markers = [
  "KW health-before-publication + typed CSpace lookup: PASS",
  "KW failure unpublishes before exact VM-graph revoke: PASS",
  "KW generation-2 restart/rebind + stale denial: PASS",
  "KW live boot-service lifecycle + reclamation: PASS",
  "KernelWorld TCG live boot-service smoke: PASS",
]

[[gate]]
id = "kernel-world-mode23-execution-device"
required = true
milestone = "KernelWorld Mode 23"
script = "ocore/kernel/smoke-kernel-world-execution-device-qemu.sh"
evidence_class = "portable_tcg"
required_tools = ["bash", "cargo", "rustc", "clang", "lld", "python3", "qemu-system-x86_64"]
positive_claims = [
  "QEMU TCG emulates SVM/NPT guest entry, VMMCALL-derived health, one validated virtual PIO request/reply, and an NPF VMEXIT",
  "Quiesce releases execution pins before endpoint and supervisor teardown; generation 2 rebinds while stale generation 1 and cross-world authority fail closed",
]
nonclaims = [
  "Mode 23 does not boot Linux, Plan 9, firmware, or a supplied user image",
  "It is not KVM, physical-hardware, PCI assignment, DMA/IOMMU, interrupt-remapping, or hardware-reset evidence",
  "It has no general guest agent, shared queue or ring, asynchronous guarantee, or SMP guarantee",
]
expected_markers = [
  "KW exact boot-to-SVM/device binding: PASS",
  "KW VMEXIT-derived health publication: PASS",
  "KW guest PIO exit + virtual endpoint reply: PASS",
  "KW NPF-driven quiesce before supervisor failure: PASS",
  "KW generation-2 guest/device rebind + stale denial: PASS",
  "KW supervised execution-device reclamation: PASS",
  "KernelWorld TCG supervised execution-device smoke: PASS",
]

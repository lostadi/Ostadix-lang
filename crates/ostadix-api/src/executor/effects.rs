//! Compatibility exports for the shared semantic effect model.
//!
//! New code should import these definitions from [`crate::effects`]. This
//! module remains so existing `o_lang::executor::effects` paths keep working.

pub use crate::effects::*;

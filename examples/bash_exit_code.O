#!/usr/bin/env O

# Non-zero exit codes surface as runtime errors (for example, `bash^(exit 1)_bash`),
# so this example stays on the success path for the smoke test.
bash{cap=backend}^(
echo "ok"
)_bash{cap=backend}

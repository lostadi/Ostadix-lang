# sql_shim.py executes raw SQL text against sqlite3. It does not bind O values
# as SQL parameters, so this example uses the literal value instead of `$val`.
let val = python^(
__oval_result__ = 100
)_python

sql^(
SELECT 100 * 2 AS doubled
)_sql
